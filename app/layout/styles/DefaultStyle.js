import {
    StyleSheet
} from 'react-native'
import app_colors from '../../_config/app_colors';

export default StyleSheet.create({
    screenContainer: {
        flex: 1,
        backgroundColor: app_colors.screen_background
    }
});