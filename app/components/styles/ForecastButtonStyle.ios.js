import {
    StyleSheet
} from 'react-native'

export default StyleSheet.create({
    container: {
        borderWidth: 2,
        borderRadius: 5,
        margin: 5,
        marginTop: 16,
        padding: 10,
        backgroundColor: "#E5A33B",
        justifyContent: "center",
        alignItems: "center"
    },
    text: {
        fontSize: 14,
        color: "#FFFFFF"
    }
});